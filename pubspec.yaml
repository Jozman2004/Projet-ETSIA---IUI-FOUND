name: l
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9  # Pour l'accès à la caméra
  path_provider: ^2.0.12 # Pour trouver les chemins de fichiers
  path: ^1.8.2 # Pour manipuler les chemins de fichiers


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
